export type UserProps = {
    id: string;
    fullName: string;
    email: string;
    celPhone: string;
    addres: string;
    number: string;
}

export type userLogin = {
    email: string;
    password: string;
}